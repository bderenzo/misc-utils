#!/usr/bin/env bash

if [ -z "${1}" ]; then
    echo "Usage: $(basename $0) URL"
    echo 'Retrieve http(s) status code from a page'
    exit 1
fi
uri="${1}"
wget --spider -S "${uri}" 2>&1 | grep "HTTP/" | grep -o '[0-9][0-9][0-9]' | tail -n1

